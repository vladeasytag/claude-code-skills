# Secrets — never commit
secrets.env
*token*
credentials.json
*.key
*.pem

# Populated data / state — ship code, not data
data/
*.db
*.db-shm
*.db-wal
logs/
.fallback_alert

# Python
*.pyc
__pycache__/
venv/
.venv/
