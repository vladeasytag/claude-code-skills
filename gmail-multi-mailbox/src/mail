#!/usr/bin/env bash
# Convenience wrapper for the PRIMARY mailbox.
cd "$(dirname "$0")" || exit 1
export PYTHONWARNINGS="ignore::FutureWarning"
export MAIL_ACCOUNT="primary"
exec ./venv/bin/python gmailer.py "$@"
